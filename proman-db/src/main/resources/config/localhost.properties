# environment properties for local development

server.host = localhost
server.port = 5432

database.name = promanj
database.user = postgres
database.password = admin

#mvn clean install -Psetup -DskipTests
